# SUPERSEDED — tests moved to tests/unit/test_no_string_heuristics.py
#
# This file used the old ConclusionBuilder.build() contract (no intent_snapshot)
# and the old SearchSidecar.should_search() 2-arg API.
# Both contracts have changed; the canonical tests live in the unit/ directory.
