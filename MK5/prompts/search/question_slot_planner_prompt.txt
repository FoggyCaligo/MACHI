[사용자 질문/요청]
{user_input}

[현재 의도]
{inferred_intent}

[현재 활성화된 핵심 용어]
{target_terms}

[현재 활성 노드 요약]
{active_nodes}

[현재 내부 판단 요약]
{current_summary}

반환 규칙:
- JSON object만 출력한다.
- entities는 질문이 실제로 다루는 개념 대상만 넣는다.
- aspects는 질문이 요구하는 비교/설명 축만 넣는다.
- entities/aspects 모두 중복 없이 짧은 명사구로 쓴다.
