[사용자 질문/요청]
{user_input}

반환 규칙:
- JSON object만 출력한다.
- entities는 사용자 질문의 실제 정보요구 대상만 넣는다.
- search_aspects는 질문 안에서 직접 확인되거나 거의 같은 표현으로 드러난 factual aspect만 넣는다.
- 질문에 직접 드러나지 않은 새 aspect를 만들지 마라.
- 확실하지 않은 aspect는 넣지 말고 빈 배열로 둔다.
- entities/search_aspects는 모두 중복 없이 짧은 명사구로 쓴다.
