[사용자 질문/요청]
{user_input}

[현재 의도]
{inferred_intent}

판정 기준:
- 지금 필요한 것이 외부 검색/외부 공적 근거 확인이면 needs_external_search=true
- 현재 대화/현재 그래프/내부 반응만으로 처리하는 편이 맞으면 needs_external_search=false

반환 규칙:
- JSON object만 출력한다.
- scope는 world_grounding / local_graph_only / mixed_but_local_answer 중 하나를 우선 사용한다.
- reason은 한두 문장으로 짧게 쓴다.
- confidence는 low / medium / high 중 하나로 쓴다.
