[사용자 질문/요청]
{user_input}

[현재 의도]
{inferred_intent}

[requested slots]
{requested_slots}

[comparison axes]
{comparison_axes}

[search evidence]
{evidence_payload}

반환 규칙:
- JSON object만 출력한다.
- 모든 requested slot에 대해 slot_support 항목을 하나씩 만든다.
- supported=true는 evidence passage에서 직접 확인되는 경우에만 허용한다.
- evidence_indices에는 실제로 근거가 된 evidence 번호만 넣는다.
- entity 검색 결과만으로 aspect까지 자동으로 supported 처리하면 안 된다.
