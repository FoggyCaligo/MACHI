[사용자 질문/요청]
{user_input}

[현재 의도]
{inferred_intent}

[requested slots]
{requested_slots}

[comparison axes]
{comparison_axes}

[현재 내부 판단 요약]
{current_summary}

[search evidence summaries]
{evidence_summaries}

반환 규칙:
- JSON object만 출력한다.
- covered_slot_labels에는 evidence에서 직접 확인되는 slot만 넣는다.
- missing_slot_labels에는 아직 evidence로 확인되지 않는 slot을 넣는다.
- search evidence는 entity 검색으로 모인 결과일 수 있으므로, aspect coverage는 evidence 문맥 안에서 실제로 확인될 때만 올린다.
