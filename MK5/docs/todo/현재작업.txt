현재작업 (단기 실행)
업데이트: 2026-04-20

[완료]
1. revision marker edge 기반 deactivation/merge 실행 규칙 강화
2. connect_type/family 기반 contradiction rule 확장
3. evidence(신뢰도/출처/신호강도) 가중치 기반 revision gate 반영
4. temporary edge(session_temporary) topic-shift 자동 정리
5. revision rule analytics + tuner + report 도구 추가
6. ChatPipeline에서 revision rule override JSON 자동 로딩/주입 연결
7. Think→Search 루프 구조화 (최대 3회, for-else 패턴)
8. 루프 내 Ollama 타임아웃 3배 상향
9. ConclusionView 도입 (MK1 원설계 복원)
   - core/entities/conclusion_view.py
   - core/thinking/conclusion_view_builder.py
   - Verbalization 계층 전체 ConclusionView 기반으로 교체
   - CoreConclusion 루프 내부 전용으로 격리
10. ModelFeedbackService 제거 (효용 대비 LLM 비용 과다)
11. SearchScopeGate LLM→임베딩+코사인 교체
    - tools/ollama_client.py: OllamaEmbedResult + embed() 추가
    - core/search/search_scope_gate.py: 임베딩+코사인 유사도 기반 판정으로 전면 교체
    - config.py: EMBEDDING_MODEL_NAME / EMBEDDING_TIMEOUT_SECONDS / SCOPE_GATE_SIMILARITY_THRESHOLD 추가
    - SearchSidecar: _can_attempt_scope_gate() 채팅 모델 무관으로 변경

[이번 턴 반영]
1. ModelFeedbackService 제거
   - chat_pipeline.py: import/인스턴스/LLM 호출/graph_commit/debug 항목 모두 제거
   - config.py: MODEL_FEEDBACK_* 3개 설정 제거
2. SearchScopeGate 임베딩 교체
   - 입력: 사용자 메시지 + 활성 노드 텍스트 (max 30개)
   - 판정: max(cosine(query_emb, node_emb)) ≥ SCOPE_GATE_SIMILARITY_THRESHOLD(0.65) → 검색 불필요
   - 모델: nomic-embed-text (Ollama /api/embed 엔드포인트)
   - fail-open: 임베딩 실패 시 SearchScopeGateError → scope_gate_error 노출 후 SlotPlanner로 진행
3. 테스트 업데이트
   - test_search_orchestration.py: FakeScopeGate/ErrorScopeGate 시그니처 수정
   - test_search_grounding_guardrails.py: SEARCH_SCOPE_GATE_TIMEOUT_SECONDS → EMBEDDING_TIMEOUT_SECONDS

[다음 우선작업]
1. ConclusionViewBuilder 키워드 매칭 정교화 (의미적 유사도 확장)
2. E2E 통합 테스트: 루프 3회 + ConclusionView 흐름 검증
3. override 프로필 전환과 실험(A/B) 경로 정리
4. Linux cron 대응 스크립트/운영 가이드 추가

[리스크/메모]
1. ConclusionViewBuilder의 node 필드 접근은 getattr()로 방어적으로 처리
2. Windows에서 pytest temp/cache 권한 이슈(`WinError 5`)가 간헐적으로 발생
3. 필요 시 `py_compile` + 스모크 검증을 병행
4. nomic-embed-text 미설치 시 ScopeGate fail-open(SearchScopeGateError) → SlotPlanner 경로로 폴백
   - Ollama에 사전 pull 필요: `ollama pull nomic-embed-text`
