현재작업 (단기 실행)
업데이트: 2026-04-16

[문서 기준 정리 완료]
1. `edge_type` 중심 설계 폐기 방향 확정
2. `relation_detail.kind` / `connect_semantics` 중심 의미론 폐기 방향 확정
3. `connect_type = flow / neutral / opposite / conflict` 기준 정리
4. 일반 node + edge 구조 중심으로 문서 기준 재정렬
5. local activation / direct access / bounded log / 기준시점 재형성 방향 정리

[현재 코드 관찰]
1. revision/promotion/model-edge-assertion 쪽 진척은 있음
2. 하지만 일부 코드/문서가 다시 `kind` 문자열 ontology에 기대기 시작함
3. revision/event 흐름과 현재 상위 철학 사이에 불일치가 생김

[다음 우선작업]
1. 코드 전반의 `kind` 의존 제거
2. conflict edge 생성/강화 정책 정리
3. direct access 계약 정리
4. bounded log 기반 기준시점 재형성 계약 정리
5. ingest에서 `concept/flow`, `concept/neutral`, `relation/conflict` 형성 강화

[리스크/메모]
1. override/revision 운영 도구가 현재 철학을 덮어쓰지 않도록 주의 필요
2. Windows에서 pytest temp/cache 권한 이슈(`WinError 5`)가 간헐적으로 발생
3. 필요 시 `py_compile` + 스모크 검증을 병행
