제거 대상 파일별 휴리스틱 목록 + 제거 후 남겨야 할 책임

이제 본론으로 갈게.
지금 단계에서 “무엇을 없애고, 무엇만 남겨야 하는지”를 파일별로 정리하면 이렇다.

1) project_analysis/services/project_profile_evidence_service.py
지금 하는 일
project 파일 중 profile 관련 문서를 선택
그 문서들에서 evidence 추출
evidence 저장
project 기반 profile 답변 생성
지금 제거해야 할 것
PROFILE_DOC_EXTENSIONS
PROFILE_NAME_HINTS
PROFILE_QUESTION_KEYWORDS
is_profile_question()의 키워드 포함 판정
_select_documents()의 확장자/파일명 힌트 기반 선택
왜 제거해야 하나

이 파일은 지금
“어떤 문서가 profile source인가”
“이 질문이 project profile 질문인가”
를 문자열로 판단하고 있다.

이건 project 축의 핵심 판단이므로
인지 핵심부에 해당한다.
따라서 문자열 휴리스틱으로 두면 안 된다.

제거 후 남겨야 할 책임

이 서비스는 앞으로 딱 이것만 맡아야 한다.

project 쪽 profile extraction orchestration
이미 선택된 source들을 받아 extractor 호출
evidence 저장
evidence 기반 answer 생성

즉 이 파일은 판별기가 아니라 오케스트레이터가 되어야 한다.

앞으로 들어와야 할 대체물
“project profile question인지”를 판단하는 의미 기반 intent resolver
“project 내 어떤 파일/문서가 profile extraction source인지”를 고르는 의미 기반 source selector
2) project_analysis/retrieval/project_retriever.py
지금 하는 일
project chunk 검색
지금 제거해야 할 것
_tokenize()
stopword 제거
_path_bonus()
_score_chunk()
질문 유형별 path 가산점
파일 경로/토큰 포함 기반 랭킹
왜 제거해야 하나

여긴 지금 완전히 문자열 기반 grep 강화판이다.
질문 의미를 이해하지 않고,
토큰 포함 여부와 파일명/path에 점수를 주고 있다.

이건 네가 금지한 “문자열 휴리스틱 중심 retrieval”의 대표 사례다.

제거 후 남겨야 할 책임
project chunk 집합을 받아
의미 기반으로 관련 chunk를 반환하는 것

즉 책임은 유지하되, 판단 방식만 바꿔야 한다.

앞으로 들어와야 할 대체물
chunk embedding 저장
query embedding과 chunk embedding의 similarity 검색
필요하면 file-level summary embedding + chunk-level embedding 2단 구조
3) memory/retrieval/update_retriever.py
지금 하는 일
일반 채팅에서 update plan 비슷한 걸 만든다
지금 제거해야 할 것
correction 키워드 감지
STATE_HINTS
길이 20자 이상이면 new_episode
문자열 포함으로 state/action 분류
왜 제거해야 하나

이 파일은 현재 일반 채팅 memory 추출의 핵심 진입점인데,
거의 전부가 문자열 규칙이다.

즉 일반 채팅 memory의 핵심 해석기가
지금 가장 휴리스틱 덩어리인 상태다.

제거 후 남겨야 할 책임

이 파일은 앞으로

raw user message
assistant reply
현재 topic/context

를 바탕으로
구조화된 update plan을 반환하는 역할만 해야 한다.

즉 남겨야 할 책임은:

action types
state payload
memory candidate
correction candidate
episode candidate
를 구조화하는 것

하지만 그 판단은 규칙이 아니라
모델 기반 structured extraction이어야 한다.

앞으로 들어와야 할 대체물
chat evidence extractor
structured JSON 출력
upstream model call 결과를 정책층으로 넘김
4) memory/policies/extraction_policy.py
지금 하는 일
update plan을 받아 실제 profile/correction/episode/state 쓰기용 구조를 만든다
지금 제거해야 할 것
STATE_RULES
_is_explicit_correction()의 correction token 검사
len(cleaned) >= 20 episode 생성
classification["route"] == "confirmed" 같은 현재 불일치성 전제
raw user_message를 직접 해석하는 부분
왜 제거해야 하나

이 파일은 원래 정책층이어야 하는데,
지금 일부는 여전히 언어 해석기처럼 작동한다.

그건 역할이 틀렸다.

제거 후 남겨야 할 책임

이 파일은 앞으로 딱 이것만 해야 한다.

upstream이 준 structured update plan을 소비
topic attach/create 유지
각 route를
discard
general
candidate
confirmed
로 정리
어떤 store write가 필요한지 결정

즉 이 파일은 해석하지 말고 배치만 해야 한다.

5) memory/policies/memory_classification_policy.py
지금 하는 일
chat/evidence route 판단
promotion 판단
지금 제거해야 할 것
len(cleaned) < 20 discard
explicit_self_statement면 candidate 같은 과도한 단일 규칙
입력이 비어 있지 않으면 default repeated_behavior로 보정하는 식의 암묵적 추정
structured signal이 부족한데도 route를 억지로 정하는 fallback
왜 제거해야 하나

이 파일은 원래 네 말대로 정책층이어야 한다.
그러려면 “무슨 의미인가”를 추정하면 안 된다.

제거 후 남겨야 할 책임
upstream이 준 structured signals를 받아
route/promotion/threshold만 결정

즉 여기선 허용되는 건:

confidence threshold
diversity 조건
evidence count
distinct source count
direct_confirm 여부
source_strength enum

같은 구조화된 정책 입력이다.

중요한 점

여기서 source_strength 자체를 쓰는 건 허용 가능하다.
단, 그 값은 여기서 추론하면 안 되고
upstream extractor가 의미 기반으로 이미 결정해서 넘겨준 값이어야 한다.

6) memory/retrieval/response_retriever.py
지금 하는 일
응답 생성용 context assemble
지금 제거해야 할 것

이 파일은 다른 파일들보다 휴리스틱이 심하진 않다.
다만 조심해야 할 건:

profile_store.search(user_message, ...)
correction_store.search(user_message, ...)
summary_store.search(user_message, ...)
episode_store.find_relevant(user_message, ...)

이 backend가 문자열 기반이면,
실질적으로 여기도 문자열 retrieval이다.

남겨야 할 책임

이 파일은 계속

profiles
corrections
summaries
episodes
states
recent_messages
를 모아서 context로 만드는 역할만 하면 된다.

즉 여기 자체는 context assembler로 두고,
실제 relevance 판단은 store/retriever 하위층에서 의미 기반으로 바뀌어야 한다.

7) tools/reply_guard.py
지금 상태

이 파일은 이미 많이 정리됐다.
현재는 문자열 휴리스틱으로 답을 찍어내는 수준은 아니다.

제거해야 할 것

현재 기준으로는 꼭 먼저 걷어낼 건 많지 않다.

남겨야 할 책임
direct source 있는지
project scope인지
recent source 있는지
같은 구조적 guard context만 만드는 것

즉 지금처럼 “판단 보조용 구조 정보”까지만 남기는 게 맞다.

8) app/api.py
지금 상태

여긴 지금 문자열 해석 문제는 거의 없다.

남겨야 할 책임
입출력 수신
upload / project_id / model 전달
request orchestrator 연결

즉 API는 계속 얇게 두는 게 맞다.

우선 제거 순서

내 기준 우선순위는 이거다.

1차 제거 대상

가장 먼저 걷어내야 하는 곳

project_analysis/retrieval/project_retriever.py
memory/retrieval/update_retriever.py
memory/policies/extraction_policy.py
project_analysis/services/project_profile_evidence_service.py 안의 profile question/file selection 문자열 판별

이 네 군데가 가장 크다.

2차 제거 대상

그 다음 정리할 곳

memory/policies/memory_classification_policy.py
memory/retrieval/response_retriever.py 하위 search backend




















실제 작업 순서 제안

내 추천 순서는 이거다.

1단계

project_analysis/retrieval/project_retriever.py

이유:

가장 노골적인 문자열 기반 retrieval이라
제거 기준을 세우기 쉽다
2단계

memory/retrieval/update_retriever.py

이유:

일반 채팅 경로가 지금 가장 오염되어 있는 입구이기 때문
3단계

memory/policies/extraction_policy.py

이유:

2단계에서 structured input이 비면
이쪽도 바로 policy-only로 줄여야 하기 때문
4단계

project_analysis/services/project_profile_evidence_service.py

이유:

project profile question / source 선택의 문자열 판별 제거
5단계

memory/policies/memory_classification_policy.py

이유:

여기까지 오고 나서야 route/promotion 정책을 다시 얇게 정리할 수 있음
6단계

response_retriever.py와 각 store search backend

이유:

retrieval 통일은 마지막에 정리하는 게 안정적













이번 배치의 중요한 한계

여기는 꼭 솔직히 말할게.

지금 구조엔 chat-side separate general/candidate store가 아직 없다.
그래서 이번 배치에서는 candidate-level chat memory만 profile로 쓰고,
general은 아직 독립 저장층으로 구현하지 않았다.

이건 의도적이다.

왜냐하면 지금 general까지 기존 profiles 테이블에 그냥 넣어버리면
네가 원하지 않는 방식으로 기본 주입 surface에 섞여 들어갈 위험이 크기 때문이다.

즉 지금은:

잘못 저장해서 오염시키는 것
보다
일단 candidate만 저장하는 것

을 택했다.

그래서 이 단계는 휴리스틱 제거 + 잘못된 저장 방지까지이고,
general/candidate/confirmed를 진짜 계층으로 만드는 건 다음 단계다.